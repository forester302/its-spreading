/// This will need a major upgrade later or specify this object to be for songs
// Checks the current room



if room == (rm_game2){
    if (!audio_is_playing(snd_temp_theme))
        audio_play_sound(snd_temp_theme, 10, true);
}

